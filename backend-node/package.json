{
    "scripts": {
        "build": "babel src -d dist",
        "start": "rm -rf ./dist && npm run build && node ./dist/app.js"
    },
    "dependencies": {
        "body-parser": "^1.18.2",
        "express": "^4.15.3",
        "lodash": "^4.17.4",
        "mysql": "^2.13.0"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-env": "^1.6.1"
    }
}
